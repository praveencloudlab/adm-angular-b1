import { AppChangeColorDirective } from './app-change-color.directive';

describe('AppChangeColorDirective', () => {
  it('should create an instance', () => {
    const directive = new AppChangeColorDirective();
    expect(directive).toBeTruthy();
  });
});
